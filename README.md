# 元原则库

一个以元认知和第一性原理为核心的个人原则管理 Web 应用。

## 功能

- 管理工作、生活、健康、家庭关系等维度下的重要原则
- 新增、编辑、删除原则
- 按维度筛选和关键词搜索
- 查看今日校准原则
- 记录原则复盘
- 使用浏览器 `localStorage` 本地保存数据
- 导出 JSON 备份

## 使用方式

直接用浏览器打开 `index.html`。

数据保存在当前浏览器当前来源的 `localStorage` 中，建议定期使用页面右上角的“导出 JSON”备份。

## 文档

需求文档见 `docs/requirements.md`。
